name: GitHub Actions Build and Deploy Demo
on:
  push:
    branches:
      - master
jobs:
  build-and-deploy:
    runs-on: ubuntu-latest
    steps:
    - name: Checkout Repository
      uses: actions/checkout@2.304.0
    - name: Create Pull Request
      uses: varsha-fexle/Demo/create-pull-request@2.304.0
